import React, { Component } from 'react';
import './App.css';
import  api  from './api';
import FaTimesCircle from 'react-icons/lib/fa/times-circle';


const { getBooks, updateBooks, deleteBooks } = api;


class App extends Component {  
  
  state = {
    books: []
  };
  
  
  componentDidMount() {    
    
    getBooks()
      .then((books) => {
        this.setState((state) => ({
          books:       (JSON.stringify(this.state.books))
        }) )
      })
    console.log(this.state.books)    
  };

  

  render() {
    return (

      <div className="App">
        <ul>{
          this.state.books.map((id,author) =>(
            <li key={id}>
              {author}
            </li>
            ) )
          
        }
          
        </ul>
          <div className='book-tile'>
          <h1>
            [title]
          </h1>
          <p>
            This book was written by [author] in [date]
          </p>
          <img src='' alt=''/>
          <button className=''>edit icon</button>
          <button className='book-tile-delete-button'></button>
        </div>
        
      </div>
    );
  }
}

export default App;
